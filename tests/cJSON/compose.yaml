services:
  afl:
    container_name: cjson-afl-test
    build: .
    command: sleep infinity
    working_dir: /workspace
    ports:
      - "8050:8050"
    volumes:
      - cjson-afl-out:/workspace/artifacts/out
volumes:
  cjson-afl-out: