services:
  ollama:
    gpus: all 
    image: ollama/ollama:latest
    container_name: ollama
    restart: always
    ports:
      - "11434:11434"
    volumes:
      - ollama:/root/.ollama
      - ./Modelfile:/root/Modelfile
volumes:
  ollama: